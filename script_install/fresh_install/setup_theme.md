## Setup Theme

- Run `restore-symlink.sh` to have the links placed in the correct placed
- Source `bashrc`
- Run `themeSet <catppuccin>`
  - will create every folder needed setting the theme as well
  - restart every app linked with themeSet
